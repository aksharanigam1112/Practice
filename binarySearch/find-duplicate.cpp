// Given an array nums containing n + 1 integerswhere each integer is between 1-n  
// prove that at least one duplicate number must exist.
// Duplicate can repeat multiple times
#include<iostream>
using namespace std;

//Hare & tortoise Algo 

int main()
{
    int arr[]= {3,1,3,4,2};
    int size = sizeof(arr)/sizeof(arr[0]);
}